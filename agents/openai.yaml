interface:
  display_name: "PowerShell Safe Skills"
  short_description: "PowerShell 7 automation with controlled legacy fallback"
  default_prompt: "Use $powershell-safe-skills to execute this Windows task on PowerShell 7, apply an isolated fallback only if necessary, and verify the outcome."
